name: Test AtlasCLI action
on:
  push:
jobs:
  test:
    name: Use AtlasCLI
    runs-on: ubuntu-latest
    env:
      MONGODB_ATLAS_PUBLIC_API_KEY: ${{ secrets.PUBLIC_API_KEY }}
      MONGODB_ATLAS_PRIVATE_API_KEY: ${{ secrets.PRIVATE_API_KEY }}
      MONGODB_ATLAS_ORG_ID: ${{ secrets.ORG_ID }}
      MONGODB_ATLAS_PROJECT_ID: ${{ secrets.PROJECT_ID }}
    steps:
      - name: AtlasCLI
        id: cli
        uses: nicohrubec/atlas-github-action@composite-action-poc
      - name: AtlasCLI version set
        uses: nicohrubec/atlas-github-action@composite-action-poc
        with:
          version: v1.9.0
      - name: Use AtlasCLI
        shell: bash
        run: atlas project ls # List projects available in your Atlas organization
