name: 'Atlas CLI GitHub Action'
description: 'Use AtlasCLI in your GitHub workflow'
branding:
  icon: 'terminal'
  color: 'green'
runs:
  using: 'docker'
  image: 'docker://mongodb/atlas:latest'
  entrypoint: 'entrypoint.sh'
  args:
    - 'atlas --help'
